package com.automation.PartialComplexScripts;

import java.awt.Image;
import java.awt.image.BufferedImage;
import java.io.File;

import javax.imageio.ImageIO;

public class readQRCode {
   
	/*step 1: Have the QR code image ready in your local machine, download below Image. 
	I have stored cherchertech as the text in the bar code.*/
	
	File file=new File("C:\\PATH\\Desktop\\qrcode-selenium.png");
	//BufferedImage bufferedImage=ImageIO.read(file);
	//LuminanceSource source = new BufferedImageLuminanceSource(bufferedImage);
	//BinaryBitmap bitmap = new BinaryBitmap(new HybridBinarizer(source));
	//Result result=new MultiFormatReader().decode(bitmap);
	//System.out.println(result.getText());
}
